## Discovery 2.3.28

- Fix a release-blocking packaging regression introduced with the Discovery 2.3.26 physical-contract runtime by preserving executable permissions for `discovery_contract_entrypoint.py` and `physical_contract_prepare.sh`.
- Add both physical-contract entrypoints to the production Home Assistant app Dockerfile chmod contract as defence in depth.
- Make the production physical-contract regression invoke the entrypoint directly instead of bypassing its executable bit through `python3`, and remove the test-side permission repair that previously masked the packaged failure.
- Add permanent packaged-runtime assertions that both physical-contract entrypoints are executable after `runtime.tar.gz` is materialized and extracted.
- Keep discovery parsing, physical topology, SNMP polling, generated YAML/dashboard semantics, Credits v25 preview behaviour, and Support My Switch data handling unchanged.
