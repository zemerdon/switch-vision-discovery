## 2.3.43

- Fix mixed-device generated-card cardinality so all resolved SNMP switch members passed to card generation are counted, including valid fallback-card devices whose registry `dashboard_support` is false.
- Prevent false degraded Discovery results from blocking the SNMP2MQTT restart/handoff and leaving newly generated switch targets inactive.
- Add a permanent HP 3500yl + two Dell N2128PX-ON mixed-device regression from field evidence.
