## 2.4.18

- Fix Calibration Profiles `Delete All Inactive` so it no longer races a selection-clear re-render.
- Keep inactive-profile multi-selection intact when clicking elsewhere on an inactive row; only the checkbox changes inactive selection.
- Add permanent regression coverage for both fixes.
- Align the Installer appearance preview with the real shared 64%-to-100% ten-step content-width scale so the Hub preview matches Installer rendering exactly.
