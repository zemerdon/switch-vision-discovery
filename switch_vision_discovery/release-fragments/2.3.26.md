## Discovery 2.3.26

- Make exact-model physical topology authoritative across Discovery so observed SNMP/API rows remain source bindings rather than silently changing the real front-panel port count.
- Fix physical interface classification and alias de-duplication for validated HP J8693A, Dell PowerConnect 5548P, Zyxel GS1900-24E, Cisco SG350-20, and stacked Cisco WS-C3750X-48P hardware.
- Harden UniFi exact-model mapping so registered RJ45/SFP topology and proven API port maps control physical bindings; extra controller rows can no longer create phantom ports, while contradictory or incomplete exact mappings fail closed.
- Keep faceplate/calibration geometry separate from hardware topology so an oversized generic fallback can provide visual capacity without creating physical entities or bindings.
- Replace the opaque Calibration Profiles HTTP 502 bridge failure with classified, privacy-safe Core/WebSocket diagnostics covering missing Supervisor authentication, WebSocket authentication/transport/protocol failures, unavailable Core commands, and Core execution failures.
- Persist the latest Calibration Profiles bridge attempt at `/share/switch_vision/diagnostics/calibration-core-bridge.json` with a correlation ID and safe failure stage; Support My Switch includes the diagnostic through its existing privacy-processed package path without exposing tokens, raw WebSocket payloads, or calibration/profile contents.
- Add permanent production physical-contract, UniFi binding, Core-bridge, diagnostic persistence, and Support My Switch sanitizer regressions while preserving contributor evidence privacy and provenance boundaries.
